#Unity Version 3.5.x Ignores

# ignore everything in library and temp folders
/PhillipsHeadiPadBuild/Library
/PhillipsHeadPortBuild/Library
/PhillipsHeadiPadBuild/Temp
/PhillipsHeadPortBuild/Temp

# don't ignore the ignore file
!.gitignore

# we do need the .asset and .prefs files in Library folders
!/PhillipsHeadiPadBuild/Library/*.asset
!/PhillipsHeadiPadBuild/Library/*.prefs
!/PhillipsHeadiPadBuild/Library/*.asset
!/PhillipsHeadiPadBuild/Library/*.prefs

# apple stuff
# .DS_Store.meta should not exist, but every now and then it happens
.DS_Store
.DS_Store.meta

# windows stuff
# Thumbs.db.meta should not exist, but every now and then it happens
Thumbs.db
Thumbs.db.meta
